Snip is a model for creating and updating files with templated content.

The model's members represent the snippets of code and the directory to find them; 
and the model's methods are for adding those code templates to a file.
 
